package com.analyzedgg.api.domain

case class Summoner(
                     id: Long,
                     name: String,
                     profileIconId: Long,
                     revisionDate: Long,
                     summonerLevel: Int
                     )
