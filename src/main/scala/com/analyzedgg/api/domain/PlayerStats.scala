package com.analyzedgg.api.domain

case class PlayerStats(
                        minionKills: Long,
                        kills: Long,
                        deaths: Long,
                        assists: Long
                        )
