package com.leagueprojecto.api.domain

case class PlayerStats(
                        minionKills: Int,
                        kills: Int,
                        deaths: Int,
                        assists: Int
                        )
