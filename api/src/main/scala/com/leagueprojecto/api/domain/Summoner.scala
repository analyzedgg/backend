package com.leagueprojecto.api.domain

case class Summoner(
                     id: Long,
                     name: String,
                     profileIconId: Long,
                     revisionDate: Long,
                     summonerLevel: Int
                     )
